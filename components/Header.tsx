import { createRef } from "react";

const Header = () => {
  const navPortalverseRef = createRef();

  return <lottus-nav-portalverse ref={navPortalverseRef}></lottus-nav-portalverse>
}

export default Header;